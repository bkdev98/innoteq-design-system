import styled from 'styled-components'

import {
  fontFamily,
  fontSize,
  fontStyle,
  fontWeight,
  color,
  lineHeight,
  letterSpacing,
} from 'styled-system'

export default styled.label`
  ${fontFamily}
  ${fontSize}
  ${fontStyle}
  ${fontWeight}
  ${color}
  ${lineHeight}
  ${letterSpacing}
`;
