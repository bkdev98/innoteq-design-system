import styled from 'styled-components'
import {
  height,
  width,
  background,
  color,
  fontSize,
  fontFamily,
  border,
  borderRadius,
  space,
  boxShadow,
  fontWeight,
  lineHeight,
  letterSpacing,
} from 'styled-system';

export default styled.textarea`
  outline: none;
  ${height}
  ${width}
  ${background}
  ${color}
  ${fontSize}
  ${fontFamily}
  ${border}
  ${borderRadius}
  ${space}
  ${boxShadow}
  ${fontWeight}
  ${lineHeight}
  ${letterSpacing}
`;
